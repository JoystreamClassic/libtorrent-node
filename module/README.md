Getting Started:

```
git clone https://github.com/JoyStreamClassic/libtorrent-node
```

Build requires [Conan](https://www.conan.io/downloads) C/C++ package manager

After installing conan add joystream repository:

`conan remote add joystreamclassic https://api.bintray.com/conan/joystreamclassic/main True`

Install dependencies and build node module:

```
cd module
npm install
```

Running Tests:

```
npm test
```
